{
  "version": 2,
  "builds": [
    {
      "src": "Chai Dapp -- Truffle React",
      "dest": "chai-dapp",  // Custom output directory for better organization
      "command": "npm run build"  // Assuming your build command in package.json
    }
  ]
}
